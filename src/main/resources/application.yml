spring:
  application:
    name: hotel-room-allocation-service

server:
  port: 8080

logging:
  level:
    root: INFO
    com.example: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health

hotel:
  room-types:
    premium-threshold: 100
